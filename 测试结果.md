# 📊 Benchmark 性能对比报告

> 测试环境：RTX 4060 8GB 显存，Python 3.14，Ollama 0.6.2，`num_predict=200`，`temperature=0.7`。  
> 旧版 14B 因显存溢出，测试时设置 `OLLAMA_NUM_GPU=15` 强制分层运行。

## 一、基础性能对比

| 测试维度 | Qwen2.5 7B (旧) | Qwen2.5 14B (旧) | Qwen3.5 4B (新) | Qwen3.5 9B (新) |
|----------|-----------------|------------------|-----------------|-----------------|
| **推理时间 (s)** | 19.53 | 51.75 | 14.54 | 22.56 |
| **系统内存变化 (GB)** | +0.91 | +4.35 | +0.50 | +1.20 |
| **回答长度 (字符)** | 366 | 367 | 200 (截断) | 200 (截断) |
| **模型文件大小 (GB)** | 4.7 | 9.0 | 3.4 | 6.6 |
| **生成速度 (tok/s)** | ~35.0 | ~7.7 | ~39.7 | ~25.1 |
| **GPU 显存占用 (GB)** | 6.0 | 8.0 (分层后 ~5.5) | 3.4 | 6.6 |
| **首次加载时间 (s)** | 3 | 13 | 2 | 5 |
| **回答质量 (主观 1-5)** | 4 | 5 | 3.5 | 4.5 |
| **显存溢出风险** | 低 | 高 (不分层则 OOM) | 极低 | 低 |

## 二、模型规模对比

- **Qwen2.5 7B**：推理耗时 19.53s，系统内存增加约 0.91 GB，回答长度 366 字符。  
- **Qwen2.5 14B**：推理耗时 51.75s，系统内存增加约 4.35 GB，回答长度 367 字符。  
- **Qwen3.5 4B**：推理耗时 14.54s，内存变化极小，适合轻量对话。  
- **Qwen3.5 9B**：推理耗时 22.56s，内存增加约 1.20 GB，速度与质量最佳平衡。

**结论**：14B 比 7B 推理时间多出约 2.7 倍，内存占用多出约 4.4 GB，但两者回答长度相近。新版 9B 在生成同等长度文本时，速度比 14B 快 3 倍以上，内存占用仅约 1/4，且 Agent 能力更强。**日常轻量任务可优先用 4B，复杂任务用 9B，完全无需 14B。**

## 三、KV Cache 上下文长度对比（14B 旧版）

| 上下文长度 | 耗时 (s) | 内存变化 (GB) |
|------------|----------|---------------|
| 2048 | 110.86 | -1.94 |
| 4096 | 124.96 | +0.44 |
| 8192 | 135.11 | +0.80 |

**结论**：项目当前默认 4096 上下文窗口在速度和内存上处于平衡状态。在 8GB 显存环境下，推荐保持 4096，极端情况可降至 2048 以减少显存压力。

## 四、Token 生成速度对比

| 模型 | 平均 tok/s | 备注 |
|------|------------|------|
| Qwen2.5 7B | ~35.0 | 流畅 |
| Qwen2.5 14B | ~7.7 | 分层后慢 |
| **Qwen3.5 4B** | **~39.7** | 极速 |
| **Qwen3.5 9B** | **~25.1** | 全 GPU，流畅 |

**结论**：新版 9B 的 token 产出速率约为旧版 14B 的 3.3 倍，流式输出时体感流畅，且 Agent/审查能力更强。4B 速度最快，适合实时轻量交互。

## 五、首次加载时间（冷启动）

| 模型 | 冷启动时间 |
|------|------------|
| Qwen2.5 7B | ~3s |
| Qwen2.5 14B | ~13s |
| Qwen3.5 4B | ~2s |
| Qwen3.5 9B | ~5s |

**结论**：项目通过 `start.bat` 预热 9B 并设为 `keep_alive=-1` 常驻，消除后续请求的加载等待，显著提升用户体验。

## 六、回答质量（主观评估）

| 模型 | 评分 | 说明 |
|------|------|------|
| Qwen2.5 7B | 4 | 日常对话流畅，复杂任务易出现逻辑断层 |
| Qwen2.5 14B | 5 | 推理严谨，细节丰富，但速度慢 |
| Qwen3.5 4B | 3.5 | 简单对话可用，深度不足 |
| **Qwen3.5 9B** | **4.5** | 接近 14B 质量，Agent 能力更强，速度更快 |

**结论**：9B 在复杂任务上的质量接近旧版 14B，且 Agent 工具调用能力更强，是性价比最高的选择。

## 七、并发性能

- **单请求**：所有模型均可稳定运行。  
- **2 个并发请求**：14B 内存/显存占用接近上限，易触发 OOM 或 502 错误；9B 在 8GB 下仍有少量余量。  

**结论**：在 8GB 显卡环境下，建议顺序处理请求，保证系统稳定。

## 八、总体结论

在 **RTX 4060 8GB 显存** 的硬件条件下，通过对新旧四款模型的全面对比，可以得出以下最终结论：

- **Qwen3.5 9B 是当前最优选择**：生成速度约 25 tok/s，比旧版 14B 快 3 倍以上，Agent/审查能力更强，显存占用仅 6.6 GB，无需分层即可全 GPU 运行。  
- **Qwen3.5 4B 作为轻量前端**：速度约 40 tok/s，适合闲聊和意图识别，与 9B 搭配实现“快慢结合”。  
- **旧版 14B 不再推荐**：即使分层运行也仅有 7.7 tok/s，且回答质量相比 9B 无明显优势，已从项目默认配置中移除。  

本项目通过 `keep_alive=-1` 常驻 9B、`keep_alive=0` 即焚 4B 的策略，配合 `start.bat` 一键预热，实现了在消费级显卡上稳定运行的多模型协作 Agent 系统。

## 九、测试代码与日志

```bash
# 旧版测试命令
python benchmark_quant.py > quant_results.txt
python benchmark_kv.py > kv_results.txt

# 查看结果
Get-Content quant_results.txt
Get-Content kv_results.txt

# 环境信息
python --version > env_info.txt
pip list | Select-String "ollama|psutil|torch|langchain" >> env_info.txt

# 模型列表
ollama list
NAME                       ID              SIZE
qwen2.5:14b                7cdf5a0187d5    9.0 GB
qwen2.5:7b                 845dbda0ea48    4.7 GB
qwen3.5:4b                 2a654d98e6fb    3.4 GB
qwen3.5:9b                 6488c96fa5fa    6.6 GB
nomic-embed-text:latest    0a109f422b47    274 MB
关键依赖版本：Python 3.14.6，LangChain 1.3.14，Ollama 0.6.2，Torch 2.13.0。
