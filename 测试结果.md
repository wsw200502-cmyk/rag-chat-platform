

|模型/测试维度|Qwen2.5:7b|Qwen2.5:14b|
|-|-|-|
|推理时间（S）|19.53|51.75|
|系统内存变化 (GB)|+0.91|+4.35|
|回答长度 (字符)|366|367|
|模型文件大小 (GB)|5.6|10.0|
|生成速度 (tok/s)|约18.7|约7.2|
|GPU 显存占用 (GB)|6.0 GB|8.0 GB|
|首次加载时间 (s)|3s|13s|
|回答质量 (主观 1-5)|4|5|
|显存溢出风险|低|高|

模型规模对比

Qwen2.5:7b：推理耗时 19.53 秒，系统内存增加约 0.91 GB，回答长度 366 字符。

Qwen2.5:14b：推理耗时 51.75 秒，系统内存增加约 4.35 GB，回答长度 367 字符。

结论：14B 比 7B 推理时间多出约 2.7 倍，内存占用多出约 4.4 GB，但两者回答长度相近。说明 14B 在生成同等长度文本时消耗更多资源，但其回答通常更深入、推理更强。日常轻量任务可优先用 7B，复杂任务再切换 14B。

KV Cache 上下文长度对比（14B）

上下文 2048：耗时 139.90 秒，内存变化 -0.41 GB。

上下文 4096：耗时 148.52 秒，内存变化 +0.14 GB。

上下文 8192：耗时 128.04 秒，内存变化 +0.89 GB。

结论：当前默认 4096 上下文窗口在速度和内存上处于平衡状态。上下文从 2048 到 4096 耗时仅增加约 6%，内存增加 0.55 GB；而 8192 内存增幅更明显。在 8GB 显存环境下，推荐保持 4096，极端情况可降至 2048 以减少显存压力。



Token 生成速度（估算）

Qwen2.5:7b：约 6.2 tok/s      计算方法：366字符÷3字符/token÷19.53s

Qwen2.5:14b：约 2.4 tok/s     计算方法：367字符÷3字符/token÷51.75s

结论：7B 的 token 产出速率约为 14B 的 2.6 倍，流式输出时体感更流畅，适合实时交互；14B 单 token 信息密度更高，但生成速度较慢。

GPU 显存占用

Qwen2.5:7b：约 6 GB

Qwen2.5:14b：约 8 GB（几乎占满 8GB 显存）

结论：14B 在推理时显存压力极大，需配合 keep\_alive 常驻和 OLLAMA\_MAX\_LOADED\_MODELS=1 单模型策略，避免触发 OOM 或 502 错误。

首次加载时间（冷启动）

Qwen2.5:7b：约 3 秒

Qwen2.5:14b：约 13 秒

结论：14B 冷启动延迟明显，通过 keep\_alive="30m" 可让模型保持常驻，消除后续请求的加载等待，显著提升用户体验。

回答质量（主观评估）

Qwen2.5:7b：日常对话流畅，简单推理可用；复杂任务（如代码审查、需求分解）可能出现逻辑断层或重复。

Qwen2.5:14b：推理更严谨，回答细节更丰富，在多步推理、Agent 工具调用等复杂场景中优势明显。

结论：14B 在复杂任务上的质量提升是项目选用其作为审查模型和 Agent 核心的核心原因，牺牲部分速度换取可靠性。

并发性能

单请求：7B/14B 均可稳定运行。

2 个并发请求：内存/显存占用接近上限，易触发 OOM 或 502 错误。

结论：在当前 8GB 显卡环境下，建议采用顺序处理，避免并发请求，保证系统稳定。

总体结论

在 RTX 4060 8GB 显存的硬件条件下，通过对比 7B 与 14B 的推理耗时、内存占用、Token 生成速度、冷启动时间以及 KV Cache 上下文长度的影响，可以得出一个整体结论：Qwen2.5:14b 虽然推理速度比 7B 慢约 2.7 倍、内存占用多 4.4 GB，且冷启动需约 13 秒，但它在复杂推理、Agent 任务和多步审查中提供了明显更高的回答质量，因此在当前项目中被作为核心模型是合理的。 通过设置 keep\_alive="30m" 让模型常驻、配合 OLLAMA\_MAX\_LOADED\_MODELS=1 限制并发、保持上下文长度 4096，有效避免了 502 错误和显存溢出，使系统在消费级显卡上稳定运行。整体而言，这套配置与优化策略，在有限硬件条件下实现了多模型协作与 Agent 的可行性与可靠性。所以，建议日常测试非复杂需求选用7B，热启动提速极其明显，需要测试复杂问题（例生成项目代码）选用14B



测试及代码

(venv) PS D:\\my-langchain-agent> python benchmark\_quant.py > quant\_results.txt

>> python benchmark\_kv.py > kv\_results.txt

(venv) PS D:\\my-langchain-agent>

(venv) PS D:\\my-langchain-agent>

(venv) PS D:\\my-langchain-agent> ollama list

>> python benchmark\_quant.py > quant\_results.txt

>> Get-Content quant\_results.txt

>> python benchmark\_kv.py > kv\_results.txt

>> Get-Content kv\_results.txt

>> python --version > env\_info.txt

>> pip list | Select-String "ollama|psutil|torch|langchain" >> env\_info.txt

>> Get-Content env\_info.txt

NAME                       ID              SIZE      MODIFIED

qwen2.5:14b                7cdf5a0187d5    9.0 GB    44 hours ago

qwen2.5:7b                 845dbda0ea48    4.7 GB    44 hours ago

nomic-embed-text:latest    0a109f422b47    274 MB    44 hours ago

llama3.1:8b                46e0c10c039e    4.9 GB    2 weeks ago



===== 测试模型: qwen2.5:7b =====

耗时: 7.95s | 系统内存变化: 0.93GB

回答长度: 378 字符



===== 测试模型: qwen2.5:14b =====

耗时: 50.43s | 系统内存变化: 4.63GB

回答长度: 407 字符



===== 上下文长度: 2048 =====

耗时: 110.86s | 系统内存变化: -1.94GB



===== 上下文长度: 4096 =====

耗时: 124.96s | 系统内存变化: 0.44GB



===== 上下文长度: 8192 =====

耗时: 135.11s | 系统内存变化: 0.80GB

Python 3.14.6



langchain                              1.3.14

langchain-chroma                 1.1.0

langchain-classic                   1.0.8

langchain-community            0.4.2

langchain-core                       1.4.9

langchain-deepseek               1.1.0

langchain-google-vertexai      3.2.4

langchain-ollama                     1.1.0

langchain-openai                     1.3.5

langchain-protocol                   0.0.18

langchain-tavily                        0.2.18

langchain-text-splitters            1.1.2

ollama                                       0.6.2

psutil                                         7.2.2

torch                                         2.13.0



